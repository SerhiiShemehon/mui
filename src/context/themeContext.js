import { createTheme } from '@mui/material';

export const themeContext = createTheme({
  palette: {
    primary: {
      main: 'rgb(93, 95, 239)',
    },
    text: {
      primary: 'rgba(0, 0, 0, 0.87)',
    }
  }
})

